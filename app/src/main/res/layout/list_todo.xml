<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="todo"
            type="ansters.me.todosomeday.data.Todo"/>
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <View
            android:layout_width="match_parent"
            android:layout_height="1px"
            android:background="@color/todo_divider"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:completedTodo="@{todo.status}"
            style="@style/Todo">

            <CheckBox
                android:id="@+id/cbFinish"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:button="@drawable/btn_circle_checkbox"
                android:layout_marginEnd="16dp"
                app:checked="@{todo.status}"/>

            <TextView
                android:id="@+id/tvTodo"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{todo.task}"
                style="@style/TextTodo"
                tools:text="Prepare to face The mighty Millhouse Manastorm!"/>

        </LinearLayout>

    </LinearLayout>
</layout>